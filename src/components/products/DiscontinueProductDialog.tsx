"use client"

// Stub — superseded by inline AlertDialog in ProductsTable
export function DiscontinueProductDialog(_props: {
  open: boolean
  onOpenChange: (open: boolean) => void
  productId: string | null
  productName: string
}) {
  return null
}
