"use client"

// Stub — superseded by inline AlertDialog in ProductsTable
export function DeactivateInventoryDialog(_props: {
  open: boolean
  onOpenChange: (open: boolean) => void
  entryId: string | null
  productName: string
}) {
  return null
}
