"use client"

import type { Product } from "@/types/product"

// Stub — implementation in a future prompt
export default function EditProductModal(_props: {
  open: boolean
  onOpenChange: (open: boolean) => void
  product: Product | null
}) {
  return null
}
